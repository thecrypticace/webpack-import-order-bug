// Can be any library import
// Commenting this import fixes the ordering problem
import Axios from "axios"

// Can be any library import — even the same one
// Commenting this import fixes the ordering problem
import Vue from "vue"

import "./a"
